# Elm Simple CRM 1.1.0

نسخه 1.1.0 یک به‌روزرسانی تکمیلی برای آماده‌سازی انتشار پایدارتر است.

## Highlights

- تنظیمات ایمیل دوحالته: `PHP mail` و `SMTP`
- امکان ارسال ایمیل تست از بخش تنظیمات
- اصلاح DatePicker شمسی هنگام تغییر ماه
- اصلاح نمایش گزینه‌های فارسی Dropdownها
- migration امن برای آپدیت نسخه‌های قبلی بدون بازنویسی تنظیمات کاربر
- بهبود نصب‌کننده و بازیابی بکاپ برای متن‌های فارسی UTF-8
- خارج شدن `database.php` واقعی از Git و اضافه شدن فایل نمونه تنظیمات دیتابیس
- اضافه شدن `.gitattributes` برای تشخیص درست زبان پروژه در GitHub

## Upgrade

برای آپدیت از نسخه قبلی:

1. از سیستم فعلی بکاپ بگیرید.
2. فایل‌های نسخه جدید را جایگزین کنید.
3. فایل زیر را روی دیتابیس فعلی اجرا کنید:

```sql
SOURCE /path/to/crm/database/upgrade_to_1_1_0.sql;
```

4. اگر از نصب دستی استفاده می‌کنید، فایل `crm/app/config/database.example.php` را الگو قرار دهید و `crm/app/config/database.php` واقعی را فقط روی سرور خود نگه دارید.

## Notes

این نسخه تنظیمات موجود سامانه را در migration جدید بازنویسی نمی‌کند.
